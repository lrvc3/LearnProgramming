class Access {
    private String name;


}
class Employee{

    private List<Access> access;
}

class Test{
    public static void main(String[] args) {
        List<Employee> empl = new ArrayList<>();
        Set<Access> resultSet = new HashSet<>();
        for(Employee e: empl){
            List<Access> accList = e.getAccess();
            resultSet.add(acc);
        }
        empl.flatMap((Employee e )->{
            return e.getAccess();
        })
    }
}


String sent = "Time waits for none";

5 
=> 101
2, 1, 3
