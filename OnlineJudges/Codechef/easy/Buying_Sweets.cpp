#include<bits/stdc++.h>
using namespace std;


int main(){

    int t;
    while(t--){
        int n, c;
        cin >> n >> c;
        vector<int> v;
        int count = -1;
        for (int i = 0; i < n; i++)
        {
            int value;
            v.push_back(value);
            

        }
        
    }

    return 0;
}